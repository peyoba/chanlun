# 进度（一页纸）

> 更新：2026-09-06

## 做到哪了

| 里程碑 | 状态 |
|---|---|
| M0 数据层 | ✅ 完成（15 只样本股四级别入库） |
| M1 笔 + 静态图 | ✅ 完成（笔端点交叉验证 98.6%） |
| M2 线段 + 中枢 + 多级别 | ⚠ 能跑，**门禁未关**：线段 73.6%、笔中枢 77.7%（目标 ≥90%） |
| M3 买卖点 + 理由 | ⚠ 能跑，理由可读；疑似信号偏多，阈值未与本人标定 |
| M4 网页 | ⚠ 能跑；2026-09-06 修回主图、徽标改最新一条、列表默认 12 条 |
| M5 交接 | ⬜ 文档已回写现状；验收包（样本图打包）仍待做 |

## 2026-09-06 修了什么

- `.gitignore` 改为只忽略根目录 `/data/`，`chanlun/data/` 源码可入库。
- 网页主图改回 `st.plotly_chart`（去掉会白屏的 iframe + CDN Plotly）。
- `chan ui` 加 `--server.headless`，避免首次邮箱提示把启动器卡死。
- 侧栏徽标改为日线最新一条；买卖点默认最近 12 条（可切仅严格 / 全部）。
- 修复一买/一卖把笔级背驰挂到同号线段上（茅台 2023-07-31 一卖误写确认于 2004-10-26）。
- 02 验证记录与交叉验证差异解释已回填；进度不再把 M2/M3 标成已关闭。

## 怎么跑

```bash
cd ~/Desktop/web/chanlun
.venv/bin/chan ui          # 或双击 启动缠论工具.command
# 浏览器 http://localhost:8501
```

## 需要本人确认的

1. 线段第二种情况维持第 67 课、不改去对齐 chan.py #272，是否同意（同意则 M2 门禁改为「差异已解释」而非 90%）。
2. 侧栏徽标只取日线最新一条（含疑似），是否可接受。
3. M2 门禁未关之前，是否先做 M5 交接包。
