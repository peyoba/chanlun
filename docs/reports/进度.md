# 进度（一页纸）

> 更新：2026-09-05

## 做到哪了

| 里程碑 | 状态 |
|---|---|
| M0 数据层 | ✅ 完成（15 只样本股四级别入库） |
| M1 笔 + 静态图 | ✅ 完成 |
| M2 线段 + 中枢 + 多级别 | ✅ 完成 |
| M3 买卖点 + 理由 | ✅ 完成 |
| M4 网页 | ✅ 完成（本次） |
| M5 交接 | ⬜ 待做（README 已可跑通，验收包待整理） |

## 本次（M4）做了什么

- `chan ui` Streamlit 网页：股池徽标、级别切换、图层开关、买卖点详情、更新数据、加 / 移自选股、URL 参数与状态记忆。
- `启动缠论工具.command` 双击启动器。
- **修复关键 bug**：YAML 把 `00700` / `002466` / `000001` 当八进制解析，导致股池错乱、数据库混入 3 个错误代码的数据。已修复解析、清理数据、补拉正确数据（详见 `docs/reports/M4-自检报告.md`）。

## 怎么跑

```bash
cd ~/Desktop/web/chanlun
.venv/bin/chan ui          # 或双击 启动缠论工具.command
# 浏览器 http://localhost:8501
```

## 需要本人确认的

1. 侧栏徽标只取日线级别最新信号（性能权衡），是否可接受。
2. 数据库已删除 01334.HK / 00001.HK / 00448.HK（bug 产物）；若确实想要长和等，重新加入即可。
3. M4 通过后，下一步做 M5 交接（README 定稿 + 验收包）。
